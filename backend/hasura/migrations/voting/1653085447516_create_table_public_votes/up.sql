CREATE TABLE "public"."votes" ("id" integer NOT NULL, "option_id" integer NOT NULL, PRIMARY KEY ("id") , FOREIGN KEY ("option_id") REFERENCES "public"."options"("id") ON UPDATE cascade ON DELETE cascade);
